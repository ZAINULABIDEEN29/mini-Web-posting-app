<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
    <div class="bg-zinc-900 text-white border-zinc-900 min-h-screen w-full p-10  ">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block " href="/logout"> Logout</a>
        </div>
        <h2 class="text-3xl  text-white"><span class="font-light">Hello,</span> <%= user.name %> 🖐️  </h2>
        <h5 class="mb-5">you can create a new post</h5>
        <form action="/post" method="post">
            <textarea name="content" placeholder="whats on your mind" class=" block w-1/3 p-3 outline-none resize-none bg-transparent border-zinc-800 rounded-md border-2"></textarea>
            <input class="px-3 w-40 text-sm py-2 bg-blue-500 rounded-md block mt-2 " type="submit" value="Create new Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400"> Your Posts.</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post p-4  mb-2 rounded-md bg-zinc-800 border-[1px] border-zinc-700 w-1/3">
                        <h4 class="text-blue-500 mb-4"><%= user.username %></h4>
                        <p class="text-sm tracking-tight"> <%= post.content %> </p>
                        <small class="mt-2 inline-block"><%= post.likes.length%> likes</small>
                        <div class="btns flex gap-4 mt-5">
                            <a class="text-blue-500" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id)=== -1 ? "Like" : "Unlike" %>
                            </a>
                            <a class="text-zinc-600" href="/edit/<%= post._id %>">Edit</a>
                            <a class="text-blue-500" href="/comment/<%= post._id %>">Comment </a>
                        </div>
                    </div>

               <% }) %>
                
            </div>
        </div>
    </div>
</body>
</html>