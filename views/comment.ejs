<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body >
    <div class="bg-zinc-900 text-white border-zinc-900 min-h-screen w-full p-10  ">
        <div class="flex w-full justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-5 inline-block " href="/logout"> Logout</a>
        </div>
        <h5 class="mb-5">Post your comment</h5>
        <form action="/comment/<%= post._id %>" method="post">
            <textarea name="content" placeholder="whats on your mind" class=" block w-1/3 p-3 outline-none resize-none bg-transparent border-zinc-800 rounded-md border-2"><%= post.content %></textarea>
            <input class="px-3 w-40 text-sm py-2 bg-yellow-500 text-black rounded-md block mt-2" type="submit" value="Comment ">
        </form>
    
    </div>
</body>
</html>